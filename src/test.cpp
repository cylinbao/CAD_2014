#include "core.h"

extern System sys;

// Accroding to one interval(TAM with least time), select possible Tests
vector<External> System::possibleExternal(TAM_Interval *p_TAM_Interval)
{
	vector<External> vecExternal;

	return vecExternal;
}

vector<BIST> System::possibleBIST()
{
	vector<BIST> vecBIST;

	return vecBIST;
}
